@model IEnumerable<PhoneServiceModel>

<div class="row">
    @foreach (var phone in Model)
    {
        <div class="col-md-4">
            <div class="card mb-3">
                <img class="card-img-top" src="@phone.ImageUrl" alt="@phone.Brand @phone.Model">
                <div class="card-body text-center">
                    <h5 class="card-title text-center">@phone.Brand @phone.Model</h5>
                    <a asp-controller="Phones" asp-action="Details" asp-route-id="@phone.Id" class="btn btn-primary">View</a>
                    @if (ViewBag.AllowPhoneEdit == true)
                    {
                        <a asp-controller="Phones" asp-action="Edit" asp-route-id="@phone.Id" class="btn btn-warning">Edit</a>
                        <a asp-controller="Phones" asp-action="Delete" asp-route-id="@phone.Id" class="btn btn-danger">Delete</a>
                    }
                </div>
            </div>
        </div>
    }
</div>