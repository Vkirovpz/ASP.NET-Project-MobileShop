@*@model PhoneSearchQueryModel

@{
    ViewBag.Title = "All phones";
}

<form method="get">
    <div class="row mb-5">
        <div class="form-group col-md-2">
            <div class="form-group">
                <label asp-for="Brand"></label>
                <select asp-for="Brand" class="form-control">
                    <option value="">All</option>
                    @foreach (var brand in Model.Brands)
                    {
                        <option value="@brand">@brand</option>
                    }
                </select>
            </div>
        </div>

        <div class="form-group col-md-2">
            <label asp-for="SearchTerm">Model</label>
            <input asp-for="SearchTerm" class="form-control">
        </div>
    </div>
</form>

@if (!Model.Phones.Any())
{
    <h2 class="text-center">No phones found by the given criteria.</h2>
}

<partial name="_PhonesPartial" model="@Model.Phones" />


@{
    var previousPage = Model.CurrentPage - 1;
    if (previousPage < 1)
    {
        previousPage = 1;
    }

    var maxPage = Math.Ceiling((double)Model.TotalPhones / PhoneSearchQueryModel.PhonesPerPage);
}

<div class="row">
    <div class="col-md-6">
        <a class="btn btn-primary @(Model.CurrentPage == 1 ? "disabled" : String.Empty)"
           asp-controller="Phones"
           asp-action="All"
           asp-route-currentPage="@previousPage"
           asp-route-brand="@Model.Brand"
           asp-route-searchTerm="@Model.SearchTerm"><<</a>
    </div>
    <div class="col-md-6">
        <a class="btn btn-primary float-right @(Model.CurrentPage == maxPage ? "disabled" : String.Empty)"
           asp-controller="Phones"
           asp-action="All"
           asp-route-currentPage="@(Model.CurrentPage + 1)"
           asp-route-brand="@Model.Brand"
           asp-route-searchTerm="@Model.SearchTerm">>></a>
    </div>
</div>*@